# ============================================================
# Environment Variables Configuration
# ============================================================
# 
# WICHTIG: Kopiere diese Datei zu .env und passe die Werte an!
# .env ist in .gitignore und wird NICHT committed!
#

# JWT Secret für Token-Signierung (mindestens 32 Zeichen!)
# Generiere mit: openssl rand -hex 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# JWT Token Ablaufzeit in Minuten
JWT_EXPIRATION_MINUTES=60

# Initial Admin Credentials (wird beim ersten Datenbankimport erstellt)
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=ChangeMe123!

# API Base URL (für Frontend)
# Development: http://localhost:8000
# Production: https://api.firma.com
VITE_API_BASE_URL=http://localhost:8000
